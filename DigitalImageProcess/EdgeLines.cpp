#include"EdgeLines.h"

EdgeLines::EdgeLines()
{
	
}

EdgeLines::~EdgeLines()
{
	
}

//判断点是否距离线接近
bool EdgeLines::isNearby(Point point, Line line)
{
	//通过最小二乘拟合，当超过一定阈值后返回false
	//这个错误阈值计算不清楚

	return false;
}

void EdgeLines::getLine(Line* lines, int noLine)
{
	//通过获取边缘段来构造直线
	//边缘段无法获取，数据结构不一致。
	//计算方法不明确
}